<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Checkstyle ruleset for date-util.

  This is intentionally a moderate, opinionated-but-practical ruleset focused on catching real
  bugs and consistency issues (unused imports, missing braces, naming conventions) rather than
  cosmetic nitpicks. It is invoked on demand by the "code-review" GitHub Actions workflow via
  `mvn checkstyle:check`; it is not bound to the default Maven build lifecycle, so it never
  affects `mvn clean test` / `mvn clean package`.
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java"/>

    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^ *\* *([^ ]+|\{@link.*)$"/>
    </module>

    <module name="TreeWalker">
        <!-- Imports -->
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>

        <!-- Naming conventions -->
        <module name="ClassTypeParameterName"/>
        <!-- Underscores are allowed to support descriptive JUnit test method names
             (e.g. parseDate_invalidInput_throws), a widely used convention. -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="ConstantName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>

        <!-- Code correctness / clarity -->
        <module name="EqualsHashCode"/>
        <module name="NeedBraces"/>
        <module name="EmptyBlock">
            <property name="option" value="text"/>
        </module>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="OneStatementPerLine"/>
        <module name="MultipleVariableDeclarations"/>

        <!-- Whitespace -->
        <module name="WhitespaceAfter"/>
        <module name="GenericWhitespace"/>

        <!-- Modifiers -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>

        <!-- Sizes -->
        <module name="MethodLength">
            <property name="max" value="150"/>
        </module>
    </module>
</module>
